declare module '@tesseract.js-data/eng' {
  const languageData: {
    code: string;
    gzip: boolean;
    langPath: string;
  };
  export default languageData;
}

declare module '@tesseract.js-data/vie' {
  const languageData: {
    code: string;
    gzip: boolean;
    langPath: string;
  };
  export default languageData;
}
